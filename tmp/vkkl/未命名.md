```cpp
// 用户侧代码
struct device_param
{
	VkPhysicalDevice physical_device;
	struct {
		bool f1 : 1;
		bool f2 : 1;
	}flag;
	::std::span<VkDeviceQueueCreateInfo const> queue_create_infos;
	::std::span<char const* const> enabled_extensions;
};
struct allocator_param{};
struct feature2_param{};
struct timeline_semaphore_param{bool timeline_semaphore;};
struct host_query_reset_param{bool host_query_reset;};
struct cluster_culling_vrs_param{bool cluster_culling_vrs;};
struct cluster_culling_param{
	bool cluster_culling_shader;
	bool multiview_cluster_culling_shader;
};
struct allocator_param {};
auto feature2_evaluate_expression
	= branch(feature2_param{...})
	| feature(timeline_semaphore_param{...}) // optional
	| feature(host_query_reset_param{...}) // optional
	| feature(
		branch(cluster_culling_param{...}) 
		| feature(cluster_culling_vrs_param{...})
	) // optional
	;
auto device_evaluate_expression
	= root(device_param{...})
	| feature(feature2_evaluate_expression) // optional
	| feature(allocator_param{...}) // optional, support allocator_callback
	;
// 甚至可以按下面这么用
auto feature2_evaluate_expression
	= feature2_param{...}
	| timeline_semaphore_param{...}
	| host_query_reset_param{...}
	| (cluster_culling_param{...} | cluster_culling_vrs_param{...})
	;
auto device_evaluate_expression
	= device_param{...}
	| feature2_evaluate_expression
	| allocator_param{...}
	;
// 还可以先对子节点求值
auto feature2_value = evaluate(feature2_evaluate_expression)
auto device_evaluate_expression
	= device_param{...}
	| feature2_value // 求值后一般是大对象，保留引用语义
	// 或者自由一点
	| evaluate(feature2_evaluate_expression)
	| allocator_param{...}
	;
auto infos = evaluate(device_evaluate_expression);
auto handle = create_device(infos); // throw VkResult if error
// 或者直接auto handle = create_device(device_evaluate_expression);
// 实现侧
concept vulkan_object; //用于标识例如device, timeline_semaphore, ...这些vulkan节点，使其在经过若干恒等表达式或者变换表达式后依旧可以表明自己身份。（例如device -> expression -> device, timeline_semaphore -> expression -> host_query_reset，允许复杂变换）
template<class T> requires{}
using expression_result_t = xxx; // 返回vulkan_object
// expression category is [root, branch, feature]?
template<class T>concept expression = requires
{
	{ expression_result_t } -> vulkan_object
}
concept branch_expression = expression && xxx;
concept root_expression = branch_expression && xxx;
// 因提前知道所有类型，所以可以为所有branch提前分配已知大小的存储，内容包括pNext链条上的各个vulkan对象，由于pNext指针的存在，如果实现为一个struct，所以还需重写这个struct的复制，移动函数
using expression_storage_t = ... // tuple 
// static_assert(expression<infos>) infos也是一个表达式
evaluate(infos) = return infos; // identify
evaluate_impl(storage&, expression)
{
	// 检查：
	//   是否重复出现allow_duplicate == false的feature
	//   是否出现不允许的feature
	// 不递归，只检查自己这层
	check_expression_dag(expression);
	// 先求值子分支
	// 示例实现，实际模板实现可能为递归遍历子feature
	for each(branch in expression) // 不包括自己
	{
		evaluate_impl(storage[branch], branch);
	}
	// 再求值自己
	storage[expression] = VkXXXInfo{
		...
	}; // 递归终点
}
evaluate(expression)
{
	// 必须是根节点
	// 开辟最终存储，所有涉及到的
	auto storage = expression_storage_t<decltype(expression)>;
	evaluate_impl(storage, expression); // 递归求值
	return storage;
}
VkDevice create_device(device_evaluate_expression_t); // throw VkResult if error
```
 我想实现个这样的vulkan库，用来简化vulkan的构造，用户在使用的时候很方便，跟用sender一样拼接就好了，代码自动化也很方便，可以直接根据vk.xml生成前面那些param结构体和后续的create_xxx函数，这是我的思路，请你先了解