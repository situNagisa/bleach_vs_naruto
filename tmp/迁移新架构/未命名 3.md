先回答你的问题（三）：
1.  不并存，两个都不要了；在一个新的scheduler上跑（实现不定义）；由workflow驱动rendezvous。合并成一个entity_scope。
2.  指代entity secondary pool/cmd，因为当fence就绪时，或者当entity.main进入下一次循环（局部作用域析构）时，secondary pool/cmd就应该被销毁，~cmd, ~pool并不对应实际代码，而是表示在保守情况下，secondary pool/cmd应该在此处析构（可能代码执行在其它地方）。
3.  一定是未定序
4.  所有的东西都需要保留，只是改下名：global_env_renderer -> renderer，frame_env_renderer -> frame_slot；并将他们拆开。按删除处理。
5.  这个问题能否搁置，preview_controller拆进entity.main还是独立我现在也说不准。全局决策的“双缓冲+插值”应该修正（只是妥协方案），但是插值确实是个问题。新架构确实有你说的问题：
	1. 本来producer/comsumer是独立工作的，现在强行变成顺序工作了，下一次计算需要等待render完后才可以进行，这样会导致并发度降下来。
	2. 但是我又不想通过改架构的方式去解决，因为本身producer产出一个snapshot就是对应这consumer的一次消费，逻辑上是没有问题的，核心问题是在计算完成后不能立马进行下一次计算，比较符合直觉的解决方法是如下，在计算完成后马上将render spawn出，不阻塞下一次计算（如果这样的话还要考虑需不需要将循环体内的代码单独拆成一个任务，entity.main负责spawn这个任务）。
		```cpp
		auto then_render = prepare_frame_render() | then(render(frame_slot, snapshot));
		
		while (!stop)
		{
			auto input = co_await on_frame();
			auto snapshot = should_compute()
				? compute(input)
				: interpolate(input);
			spawn(just(snapshot) | then_render);
			
			// 或者
			auto input = co_await on_frame();
			spawn(just(input) | (compute || interpolate) | then_render);
		}
		```
	3. 但这样的方法会要求框架的on_frame提供一个“正帧”，“插值帧”的接口，这会把插值语义引入进框架，而实际上我更想让插值是玩家自己实现的东西，框架可以提供一个可选的插值算法，让entity的实现者一键插值。这样可以完美利用entity.main可以高度自定义的特性。但是具体实现方法我没有想到什么很优雅的。所以只是先这么想，实现仍按照保守实现，只是后续发展方向可能朝此进行。
6.  不定名。你这里理解错误了，`render scheduler`、`render_workflow`指的是同一个东西，`render workflow`，`frame workflow`是不同的东西，新的render/frame workflow视情况而定。可以删除，render-task有些与entity.main共通的内容需要转移。render-scheduler子文件夹直接整个删除。
7.  前者，需明确表明哪些未确认，部分简单或者比较重要的可以跟我交流后决定。
我发现一个之前示例代码的一个重大遗漏：
```cpp
auto workflow::frame() -> void
{
    auto input = collect_input();

    // 这里的sync_wait其实是伪概念，waiters作为一个长期协程，永远等不到结束
    // sync_wait(logic_waiters);
    auto w = swap_out(logic_waiters);
    

    // 保守但正确的 GPU 准备
    auto frame_slot = create_frame_slot();
    begin_frame(frame_slot);

    // 第二阶段：把 frame_slot 交给 entity.main 做 render
    sync_wait(render_waiters);

    end_frame(frame_slot);

    // 保守等待 GPU 完成后才结束资源生命周期
    sync_wait(when_all(frame_slot.fence, swapchain));
    ~cmd;
    ~pool;
}
```


```cpp
on_frame() 
| continues_on(pool) 
| when_all(computes) 
| continues_on(start_scheduler) 
| begin_render
| continues_on(pool) 
| when_all(renders)
| continues_on(start_scheduler) 

```